BRAVE_API_KEY = YOUR_BRAVE_API_KEY

## UNCOMMENT NECESSARY VARIABLES BELOW

# BRAVE_TARGET_QUERIES='["(GenAI | Generative AI | LLM) ( Application | Applied ) (Engineer | Developer)", "(Data) (Scientist | Science)"]'
# BRAVE_ATS_DOMAINS='["jobs.ashbyhq.com", "boards.greenhouse.io", "jobs.lever.co", "jobs.smartrecruiters.com", "apply.workable.com", "wd1.myworkdayjobs.com", "jobs.jobvite.com", "careers.icims.com", "www.levels.fyi/jobs"]'
# BRAVE_USAGE_FILE = "api_usage.json"
# BRAVE_JOB_SEARCH_LOGFILE = "brave-job-search.log"
# BRAVE_MONTHLY_LIMIT = 2000
# BRAVE_RATE_LIMIT = 1.1
# BRAVE_FRESHNESS = "pd"

# Google Custom Search API Configuration
# GOOGLE_SEARCH_API_KEY = "your_google_search_api_key"
# GOOGLE_SEARCH_ID = "your_google_search_engine_id"
# GOOGLE_SEARCH_RATE_LIMIT = 1.1  # Seconds between requests (default: 1.1)

## JOB PROCESSING CONFIGURATION

# Database Configuration
# DB_PATH = "job_search.db"
# CHROMA_PATH = "./chroma_db"

# Scraping Configuration
# SCRAPE_RATE_LIMIT = 2.0  # Seconds between requests (default: 2.0)
# REQUEST_TIMEOUT = 30     # Request timeout in seconds (default: 30)
# GLOBAL_RATE_LIMIT = 5    # Requests per minute per ATS domain (default: 5)

# Embedding Configuration
# EMBEDDING_PROVIDER = "local"  # Options: "local" (sentence-transformers) or "openai"
# EMBEDDING_MODEL = "all-MiniLM-L6-v2"  # Model name for local embeddings (sentence-transformers)

# OpenAI Embedding Configuration (for embeddings)
# OPENAI_EMBEDDING_API_KEY = "your_openai_api_key"  # Required if EMBEDDING_PROVIDER = "openai"
# OPENAI_EMBEDDING_BASE_URL = "https://api.openai.com/v1"  # Optional: Custom OpenAI-compatible endpoint for embeddings
# OPENAI_EMBEDDING_MODEL = "text-embedding-3-small"  # Model name for embeddings (default: text-embedding-3-small)
# OPENAI_EMBEDDING_DIMENSION = 1536  # Optional: Dimension for custom OpenAI-compatible endpoints (required for non-OpenAI models)

# General OpenAI Configuration (for future inference features)
# OPENAI_API_KEY = "your_openai_api_key"  # API key for general OpenAI-compatible endpoints
# OPENAI_BASE_URL = "https://api.openai.com/v1"  # Base URL for general OpenAI-compatible endpoints
# OPENAI_MODEL = "gpt-4"  # Model name for general OpenAI-compatible endpoints

# Note: The system checks OPENAI_EMBEDDING_* variables first, then falls back to OPENAI_* variables
